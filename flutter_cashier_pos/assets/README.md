# Flutter Cashier POS Assets

This directory contains assets for the Flutter Cashier POS application.

## Structure
- `images/` - Application images and photos
- `icons/` - Custom icons and SVG assets

## Usage
Assets are referenced in the pubspec.yaml file and can be loaded using:
```dart
Image.asset('assets/images/image_name.png')
```